# Vercel: add POSTGRES_URL or DATABASE_URL in Project → Settings → Environment Variables
# (Vercel Postgres integration usually sets POSTGRES_URL automatically.)
POSTGRES_URL=
DATABASE_URL=

# Optional: shown on GET /api/db-check
DB_CHECK_AUTHOR=Carter Wright
